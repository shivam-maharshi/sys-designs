package com.walmart.mts.request.handler;

/**
 * All {@link RequestHandler} objects can extend this abstract class to reuse
 * functionality. Currently holds nothing but can be used in the future.
 * 
 * @author shivam.maharshi
 *
 * @param <K>
 * @param <M>
 */
public abstract class BaseRequestHandler<K, M> implements RequestHandler<K, M> {

}
